import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vurado.maincharacter',
  appName: 'maincharacters',
  webDir: 'dist'
};

export default config;
